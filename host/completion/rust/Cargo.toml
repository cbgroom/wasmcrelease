[package]
name = "wasmc-completion-guard"
version = "0.0.1"
edition = "2021"
publish = false
[workspace]
[features]
native-readiness = ["dep:mio"]
[dependencies]
mio = { version = "=1.2.3", default-features = false, features = ["os-poll", "net"], optional = true }
getrandom = "=0.4.3"
serde_json = "1"
