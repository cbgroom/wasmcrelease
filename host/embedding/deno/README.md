# Deno embedding

Deno carries the same Host contract/runtime/driver model. Deno permissions are environment admission controls, not a second Host capability model.

A Deno provider maps granted environment capabilities into canonical Host resources and operations.
