name: Public Host and Lib end-to-end
on:
  push:
    branches: [main, work/host-lib-e2e, work/host-completion-guard, work/host-startup-identity, work/host-tcp-endpoint, work/host-tcp-read-stop, work/host-tcp-listener, work/host-resident-app, work/host-tcp-write-outcome, work/host-engine-parity, work/host-resident-benchmark, work/host-stop-failure, work/host-driver-errors, work/host-browser-parity, work/host-quarantine-supervisor, work/host-kernel-engine-parity, work/host-native-owner-supervisor, work/host-scoped-owner-lifetime, work/host-core-negotiation, work/host-tcp-half-close-probe, work/host-core-quota-safety, work/host-corelib-bytes-io]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  mobile-compile:
    strategy:
      fail-fast: false
      matrix:
        target: [aarch64-apple-ios, aarch64-apple-ios-sim, aarch64-linux-android, aarch64-unknown-linux-ohos]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.5.1
      - run: rustup toolchain install 1.96.0 --profile minimal
      - env:
          TARGET: ${{ matrix.target }}
        run: rustup target add "$TARGET" --toolchain 1.96.0
      - name: Wasmi-only cross-target check, no device or link claim
        env:
          TARGET: ${{ matrix.target }}
          SOURCE: ${{ github.sha }}
        run: |
          cargo +1.96.0 check --locked --manifest-path host/tests/e2e/rust/Cargo.toml --target "$TARGET" --all-targets
          cargo +1.96.0 tree --locked --manifest-path host/tests/e2e/rust/Cargo.toml --target "$TARGET" --prefix none > "$RUNNER_TEMP/default-dependencies.txt"
          node scripts/test-native-reference-dependencies.mjs
          sh scripts/check-native-reference-dependencies.sh "$RUNNER_TEMP/default-dependencies.txt"
          printf '%s\n' "Source $SOURCE; $TARGET Wasmi-only compile PASS; no link/sign/install/device/network-permission/runtime qualification" >> "$GITHUB_STEP_SUMMARY"
  stream-shapes:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.5.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: 1.3.14
      - uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed
        with:
          deno-version: 2.9.4
      - name: Fast stream ownership and actual JS service proof
        shell: bash
        run: |
          node host/drivers/udp/entrypoint-test.mjs
          node host/drivers/tcp/resident-snapshot-test.mjs
          bun host/drivers/tcp/resident-snapshot-test.mjs
          deno run --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms host/drivers/tcp/resident-snapshot-test.mjs
          node host/drivers/tcp/stream-snapshot-test.mjs
          bun host/drivers/tcp/stream-snapshot-test.mjs
          deno run host/drivers/tcp/stream-snapshot-test.mjs
          node host/drivers/tcp/buffer-budget-test.mjs
          bun host/drivers/tcp/buffer-budget-test.mjs
          deno run host/drivers/tcp/buffer-budget-test.mjs
          node host/runtime/completion/snapshot-test.mjs
          node host/drivers/tcp/write-snapshot-test.mjs
          node host/drivers/tcp/guard-retirement-test.mjs
          node host/drivers/tcp/quarantine-retirement-test.mjs
          node host/drivers/tcp/guard-pool-test.mjs
          node host/drivers/tcp/completion-quarantine-test.mjs --real-tcp
          bun host/runtime/completion/snapshot-test.mjs
          bun host/drivers/tcp/write-snapshot-test.mjs
          bun host/drivers/tcp/guard-retirement-test.mjs
          bun host/drivers/tcp/quarantine-retirement-test.mjs
          bun host/drivers/tcp/guard-pool-test.mjs
          bun host/drivers/tcp/completion-quarantine-test.mjs --real-tcp
          deno run host/runtime/completion/snapshot-test.mjs
          deno run host/drivers/tcp/write-snapshot-test.mjs
          deno run host/drivers/tcp/guard-retirement-test.mjs
          deno run host/drivers/tcp/quarantine-retirement-test.mjs
          deno run host/drivers/tcp/guard-pool-test.mjs
          deno run --allow-net=127.0.0.1 host/drivers/tcp/completion-quarantine-test.mjs --real-tcp
          node host/drivers/file/write-snapshot-test.mjs
          bun host/drivers/file/write-snapshot-test.mjs
          deno run host/drivers/file/write-snapshot-test.mjs
          node host/drivers/file/read-completion-test.mjs
          bun host/drivers/file/read-completion-test.mjs
          deno run host/drivers/file/read-completion-test.mjs
          node host/core/io/snapshot-test.mjs
          bun host/core/io/snapshot-test.mjs
          deno run --allow-read=standard/corelib,current,host/core/io host/core/io/snapshot-test.mjs
          node host/drivers/udp/test.mjs
          node host/drivers/udp/fault-test.mjs
          node host/drivers/udp/retirement-test.mjs
          bun host/drivers/udp/test.mjs
          bun host/drivers/udp/fault-test.mjs
          bun host/drivers/udp/retirement-test.mjs
          deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms host/drivers/udp/test.mjs
          deno run --allow-net=127.0.0.1 host/drivers/udp/fault-test.mjs
          deno run host/drivers/udp/retirement-test.mjs
          node host/drivers/tcp/server-test.mjs --js-only
          bun host/drivers/tcp/server-test.mjs --js-only --diagnose
          deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms --allow-write=target/host-tcp-app host/drivers/tcp/server-test.mjs --js-only
  composition:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm, macos-14, macos-15-intel, windows-2025, windows-11-arm]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.5.1
      - run: rustup toolchain install 1.96.0 --profile minimal --component clippy,rustfmt
      - run: rustup target add wasm32-unknown-unknown --toolchain 1.96.0
      - name: Restore locked desktop dependency compilation cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            host/tests/e2e/rust/target
            host/runtime/completion/rust/target
            host/drivers/file/rust/target
          key: host-composition-v1-${{ matrix.os }}-${{ runner.arch }}-rust1.96.0-${{ hashFiles('host/**/Cargo.lock', 'host/**/Cargo.toml') }}
      - name: Always rebuild owned Host crates after cache restore
        shell: bash
        run: |
          cargo +1.96.0 clean --locked --manifest-path host/tests/e2e/rust/Cargo.toml -p wasmc-lib-host-e2e -p wasmc-completion-guard -p wasmc-preopened-file-reference
          cargo +1.96.0 clean --locked --manifest-path host/runtime/completion/rust/Cargo.toml -p wasmc-completion-guard
          cargo +1.96.0 clean --locked --manifest-path host/drivers/file/rust/Cargo.toml -p wasmc-preopened-file-reference
      - name: Compile reviewed Rust CoreLib conformance caller
        shell: bash
        run: |
          mkdir -p target/corelib-io
          rustc +1.96.0 --edition=2024 --crate-type=cdylib --target=wasm32-unknown-unknown -O -D warnings -C panic=abort host/core/io/private-abi-guest.rs -o target/corelib-io/rust-guest.wasm
      - run: cargo +1.96.0 fmt --check --manifest-path host/runtime/completion/rust/Cargo.toml
      - run: cargo +1.96.0 clippy --locked --manifest-path host/runtime/completion/rust/Cargo.toml --all-targets -- -D warnings
      - run: cargo +1.96.0 build --release --locked --manifest-path host/runtime/completion/rust/Cargo.toml
      - run: cargo +1.96.0 test --release --locked --manifest-path host/runtime/completion/rust/Cargo.toml
      - run: node host/runtime/completion/fault-test.mjs
      - run: node host/runtime/completion/lifetime-test.mjs
      - run: node host/drivers/tcp/stop-failure-test.mjs
      - run: node host/drivers/tcp/driver-error-test.mjs
      - run: node host/drivers/file/retirement-test.mjs
      - run: node host/drivers/file/write-snapshot-test.mjs
      - run: node host/drivers/file/read-completion-test.mjs
      - run: node host/drivers/tcp/driver-retirement-test.mjs --real-tcp
      - run: node host/drivers/tcp/stream-snapshot-test.mjs
      - run: node host/drivers/tcp/buffer-budget-test.mjs
      - run: node host/runtime/completion/snapshot-test.mjs
      - run: node host/drivers/tcp/write-snapshot-test.mjs
      - run: node host/drivers/tcp/guard-retirement-test.mjs
      - run: node host/drivers/tcp/quarantine-retirement-test.mjs
      - run: node host/drivers/tcp/guard-pool-test.mjs
      - run: node host/drivers/tcp/completion-quarantine-test.mjs --real-tcp
      - run: node host/core/io/snapshot-test.mjs
      - run: node host/drivers/tcp/supervisor-test.mjs
      - run: node host/drivers/udp/test.mjs
      - run: node host/drivers/udp/fault-test.mjs
      - run: node host/drivers/udp/retirement-test.mjs
      - run: node host/drivers/udp/entrypoint-test.mjs --node-only
      - run: node host/drivers/tcp/resident-snapshot-test.mjs
      - name: Independent process startup identity
        shell: bash
        run: |
          binary=host/runtime/completion/rust/target/release/scoped-reference
          if [[ "$RUNNER_OS" == Windows ]]; then binary="$binary.exe"; fi
          node host/runtime/completion/scoped-test.mjs "$binary"
      - run: cargo +1.96.0 build --release --locked --manifest-path host/drivers/file/rust/Cargo.toml
      - name: Existing real file adapter regression
        shell: bash
        run: |
          binary=host/drivers/file/rust/target/release/wasmc-preopened-file-reference
          if [[ "$RUNNER_OS" == Windows ]]; then binary="$binary.exe"; fi
          node host/drivers/file/test.mjs "$binary"
      - name: Session, cancellation and late completion parity
        shell: bash
        run: |
          binary=host/runtime/completion/rust/target/release/wasmc-completion-guard
          if [[ "$RUNNER_OS" == Windows ]]; then binary="$binary.exe"; fi
          node host/runtime/completion/test.mjs "$binary"
      - run: cargo +1.96.0 fmt --check --manifest-path host/tests/e2e/rust/Cargo.toml
      - run: cargo +1.96.0 clippy --locked --manifest-path host/tests/e2e/rust/Cargo.toml --all-targets -- -D warnings
      - run: cargo +1.96.0 build --release --locked --manifest-path host/tests/e2e/rust/Cargo.toml
      - run: cargo +1.96.0 test --release --locked --manifest-path host/tests/e2e/rust/Cargo.toml
      - name: Half-close reply with independent Native peer
        shell: bash
        run: |
          peer=host/tests/e2e/rust/target/release/tcp-half-close-peer
          if [[ "$RUNNER_OS" == Windows ]]; then peer="$peer.exe"; fi
          node host/drivers/tcp/half-close-test.mjs "$peer"
          node host/drivers/tcp/half-close-test.mjs
      - name: Wasmi-only dependency contract
        shell: bash
        run: |
          cargo +1.96.0 tree --locked --manifest-path host/tests/e2e/rust/Cargo.toml > "$RUNNER_TEMP/wasmi-default-tree.txt"
          if grep -E 'wasmtime|cranelift' "$RUNNER_TEMP/wasmi-default-tree.txt"; then exit 1; fi
      - name: Preconnected TCP and frozen algorithm Lib
        shell: bash
        run: |
          binary=host/tests/e2e/rust/target/release/tcp-reference
          if [[ "$RUNNER_OS" == Windows ]]; then binary="$binary.exe"; fi
          node host/drivers/tcp/test.mjs "$binary"
          stop=host/tests/e2e/rust/target/release/tcp-stop-reference
          if [[ "$RUNNER_OS" == Windows ]]; then stop="$stop.exe"; fi
          node host/drivers/tcp/fault-test.mjs "$stop"
          server=host/tests/e2e/rust/target/release/tcp-server-reference
          resident=host/tests/e2e/rust/target/release/resident-app-reference
          if [[ "$RUNNER_OS" == Windows ]]; then server="$server.exe"; resident="$resident.exe"; fi
          node host/drivers/tcp/server-test.mjs "$server" "$resident"
          write=host/tests/e2e/rust/target/release/tcp-write-reference
          if [[ "$RUNNER_OS" == Windows ]]; then write="$write.exe"; fi
          node host/drivers/tcp/write-test.mjs "$write"
          udp=host/tests/e2e/rust/target/release/udp-server-reference
          if [[ "$RUNNER_OS" == Windows ]]; then udp="$udp.exe"; fi
          node host/drivers/udp/native-test.mjs "$udp"
      - name: Same App and published Lib through real file Host
        shell: bash
        run: |
          binary=host/tests/e2e/rust/target/release/wasmc-lib-host-e2e
          if [[ "$RUNNER_OS" == Windows ]]; then binary="$binary.exe"; fi
          node host/tests/e2e/test.mjs "$binary"
          core=host/tests/e2e/rust/target/release/corelib-io-reference
          if [[ "$RUNNER_OS" == Windows ]]; then core="$core.exe"; fi
          node host/core/io/test.mjs "$core"
          node host/core/io/lifetime-test.mjs "$core"
      - if: runner.os != 'Windows'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: 1.3.14
      - if: runner.os != 'Windows'
        uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed
        with:
          deno-version: 2.9.4
      - if: runner.os != 'Windows'
        name: Bun and restricted Deno composition
        shell: bash
        run: |
          node host/drivers/udp/entrypoint-test.mjs
          bun host/drivers/tcp/resident-snapshot-test.mjs
          deno run --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms host/drivers/tcp/resident-snapshot-test.mjs
          binary=host/tests/e2e/rust/target/release/wasmc-lib-host-e2e
          completion=host/runtime/completion/rust/target/release/wasmc-completion-guard
          file=host/drivers/file/rust/target/release/wasmc-preopened-file-reference
          scoped=host/runtime/completion/rust/target/release/scoped-reference
          tcp=host/tests/e2e/rust/target/release/tcp-reference
          stop=host/tests/e2e/rust/target/release/tcp-stop-reference
          server=host/tests/e2e/rust/target/release/tcp-server-reference
          resident=host/tests/e2e/rust/target/release/resident-app-reference
          write=host/tests/e2e/rust/target/release/tcp-write-reference
          peer=host/tests/e2e/rust/target/release/tcp-half-close-peer
          bun host/drivers/tcp/half-close-test.mjs "$peer"
          deno run --allow-net=127.0.0.1 --allow-env=NODE_V8_COVERAGE --allow-run="$peer" host/drivers/tcp/half-close-test.mjs "$peer"
          deno run --allow-net=127.0.0.1 host/drivers/tcp/half-close-test.mjs
          bun host/drivers/tcp/half-close-test.mjs --characterize-js-peer
          core=host/tests/e2e/rust/target/release/corelib-io-reference
          udp=host/tests/e2e/rust/target/release/udp-server-reference
          bun host/drivers/udp/test.mjs
          bun host/drivers/udp/fault-test.mjs
          bun host/drivers/udp/retirement-test.mjs
          deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms host/drivers/udp/test.mjs
          deno run --allow-net=127.0.0.1 host/drivers/udp/fault-test.mjs
          deno run host/drivers/udp/retirement-test.mjs
          bun host/drivers/udp/native-test.mjs "$udp"
          deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms --allow-write=target/host-udp --allow-env=NODE_V8_COVERAGE --allow-run="$udp" host/drivers/udp/native-test.mjs "$udp"
          bun host/core/io/test.mjs "$core"
          deno run --allow-read=current,host/core/io,standard/corelib,target/corelib-io --allow-write=target/corelib-io --allow-env=NODE_V8_COVERAGE --allow-run="$core" host/core/io/test.mjs "$core"
          bun host/core/io/lifetime-test.mjs "$core"
          deno run --allow-read=standard/corelib,target/corelib-io --allow-env=NODE_V8_COVERAGE --allow-run="$core" host/core/io/lifetime-test.mjs "$core"
          bun host/drivers/tcp/write-test.mjs "$write"
          bun host/drivers/tcp/stop-failure-test.mjs
          deno run host/drivers/tcp/stop-failure-test.mjs
          bun host/drivers/tcp/driver-error-test.mjs
          deno run host/drivers/tcp/driver-error-test.mjs
          bun host/drivers/file/retirement-test.mjs
          deno run host/drivers/file/retirement-test.mjs
          bun host/drivers/file/write-snapshot-test.mjs
          deno run host/drivers/file/write-snapshot-test.mjs
          bun host/drivers/file/read-completion-test.mjs
          deno run host/drivers/file/read-completion-test.mjs
          bun host/drivers/tcp/stream-snapshot-test.mjs
          deno run host/drivers/tcp/stream-snapshot-test.mjs
          bun host/drivers/tcp/buffer-budget-test.mjs
          deno run host/drivers/tcp/buffer-budget-test.mjs
          bun host/runtime/completion/snapshot-test.mjs
          bun host/drivers/tcp/write-snapshot-test.mjs
          bun host/drivers/tcp/guard-retirement-test.mjs
          bun host/drivers/tcp/quarantine-retirement-test.mjs
          bun host/drivers/tcp/guard-pool-test.mjs
          bun host/drivers/tcp/completion-quarantine-test.mjs --real-tcp
          deno run host/runtime/completion/snapshot-test.mjs
          deno run host/drivers/tcp/write-snapshot-test.mjs
          deno run host/drivers/tcp/guard-retirement-test.mjs
          deno run host/drivers/tcp/quarantine-retirement-test.mjs
          deno run host/drivers/tcp/guard-pool-test.mjs
          deno run --allow-net=127.0.0.1 host/drivers/tcp/completion-quarantine-test.mjs --real-tcp
          bun host/core/io/snapshot-test.mjs
          deno run --allow-read=standard/corelib,current,host/core/io host/core/io/snapshot-test.mjs
          bun host/drivers/tcp/driver-retirement-test.mjs --real-tcp
          deno run --allow-net=127.0.0.1 host/drivers/tcp/driver-retirement-test.mjs --real-tcp
          bun host/drivers/tcp/supervisor-test.mjs
          deno run host/drivers/tcp/supervisor-test.mjs
          deno run --allow-net=127.0.0.1 --allow-env=NODE_V8_COVERAGE --allow-run="$write" host/drivers/tcp/write-test.mjs "$write"
          bun host/drivers/tcp/server-test.mjs "$server" "$resident"
          mkdir -p target/host-tcp-app
          deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms,target --allow-write=target/host-tcp-app --allow-env=NODE_V8_COVERAGE --allow-run="$server,$resident" host/drivers/tcp/server-test.mjs "$server" "$resident"
          bun host/drivers/tcp/test.mjs "$tcp"
          deno run --allow-net=127.0.0.1 --allow-read=libs/wasmc-owned-algorithms --allow-env=NODE_V8_COVERAGE --allow-run="$tcp" host/drivers/tcp/test.mjs "$tcp"
          bun host/drivers/tcp/fault-test.mjs "$stop"
          deno run --allow-net=127.0.0.1 --allow-env=NODE_V8_COVERAGE --allow-run="$stop" host/drivers/tcp/fault-test.mjs "$stop"
          bun host/runtime/completion/scoped-test.mjs "$scoped"
          deno run --allow-env=NODE_V8_COVERAGE --allow-run="$scoped,$(command -v deno)" host/runtime/completion/scoped-test.mjs "$scoped"
          bun host/runtime/completion/lifetime-test.mjs
          deno run host/runtime/completion/lifetime-test.mjs
          bun host/runtime/completion/fault-test.mjs
          deno run host/runtime/completion/fault-test.mjs
          bun host/drivers/file/test.mjs "$file"
          bun host/runtime/completion/test.mjs "$completion"
          deno run --allow-env=NODE_V8_COVERAGE --allow-run="$completion" host/runtime/completion/test.mjs "$completion"
          bun host/tests/e2e/test.mjs "$binary"
          mkdir -p target/host-file-tmp
          TMPDIR="$PWD/target/host-file-tmp" deno run --allow-read=target --allow-write=target --allow-env=TMPDIR,NODE_V8_COVERAGE --allow-run="$file" host/drivers/file/test.mjs "$file"
          TMPDIR="$PWD/target/host-file-tmp" deno run --allow-read=current,libs/wasmc-owned-algorithms,host/runtime/completion,host/tests/e2e,target --allow-write=target --allow-env=TMPDIR,NODE_V8_COVERAGE --allow-run="$binary" host/tests/e2e/test.mjs "$binary"
      - name: Optional Wasmtime profile qualification
        shell: bash
        run: |
          cargo +1.96.0 clippy --locked --manifest-path host/tests/e2e/rust/Cargo.toml --features wasmtime-engine --all-targets -- -D warnings
          cargo +1.96.0 test --release --locked --manifest-path host/tests/e2e/rust/Cargo.toml --features wasmtime-engine
          cargo +1.96.0 build --release --locked --manifest-path host/tests/e2e/rust/Cargo.toml --features wasmtime-engine
          server=host/tests/e2e/rust/target/release/tcp-server-reference
          resident=host/tests/e2e/rust/target/release/resident-app-reference
          if [[ "$RUNNER_OS" == Windows ]]; then server="$server.exe"; resident="$resident.exe"; fi
          node host/drivers/tcp/server-test.mjs "$server" "$resident" --wasmtime
          core=host/tests/e2e/rust/target/release/corelib-io-reference
          if [[ "$RUNNER_OS" == Windows ]]; then core="$core.exe"; fi
          node host/core/io/test.mjs "$core" --wasmtime
          node host/core/io/lifetime-test.mjs "$core" --wasmtime
          udp=host/tests/e2e/rust/target/release/udp-server-reference
          if [[ "$RUNNER_OS" == Windows ]]; then udp="$udp.exe"; fi
          node host/drivers/udp/native-test.mjs "$udp" --wasmtime
          benchmark=host/tests/e2e/rust/target/release/resident-benchmark
          if [[ "$RUNNER_OS" == Windows ]]; then benchmark="$benchmark.exe"; fi
          node host/drivers/tcp/benchmark.mjs "$benchmark" target/host-tcp-app/guest.wasm --wasmtime
          if [[ "$RUNNER_OS" != Windows ]]; then
            bun host/drivers/udp/native-test.mjs "$udp" --wasmtime
            deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms --allow-write=target/host-udp --allow-env=NODE_V8_COVERAGE --allow-run="$udp" host/drivers/udp/native-test.mjs "$udp" --wasmtime
            bun host/core/io/test.mjs "$core" --wasmtime
            deno run --allow-read=current,host/core/io,standard/corelib,target/corelib-io --allow-write=target/corelib-io --allow-env=NODE_V8_COVERAGE --allow-run="$core" host/core/io/test.mjs "$core" --wasmtime
            bun host/core/io/lifetime-test.mjs "$core" --wasmtime
            deno run --allow-read=standard/corelib,target/corelib-io --allow-env=NODE_V8_COVERAGE --allow-run="$core" host/core/io/lifetime-test.mjs "$core" --wasmtime
            bun host/drivers/tcp/server-test.mjs "$server" "$resident" --wasmtime
            deno run --allow-net=127.0.0.1 --allow-read=current,host/tests/e2e,libs/wasmc-owned-algorithms,target --allow-write=target/host-tcp-app --allow-env=NODE_V8_COVERAGE --allow-run="$server,$resident" host/drivers/tcp/server-test.mjs "$server" "$resident" --wasmtime
            bun host/drivers/tcp/benchmark.mjs "$benchmark" target/host-tcp-app/guest.wasm --wasmtime
            deno run --allow-read=libs/wasmc-owned-algorithms,target/host-tcp-app --allow-env=NODE_V8_COVERAGE --allow-run="$benchmark" host/drivers/tcp/benchmark.mjs "$benchmark" target/host-tcp-app/guest.wasm --wasmtime
          fi
      - name: Public integrity
        if: runner.os == 'Linux'
        run: ./scripts/validate-maintainer.sh
      - name: Exact-source evidence scope
        shell: bash
        env:
          SOURCE: ${{ github.sha }}
        run: |
          printf '%s\n' "Source $SOURCE; $RUNNER_OS/$RUNNER_ARCH; 76 guard transitions, 10 fault controls, 15 guard/owner Rust tests, 40000 scoped and supervisor cycles, 8 binary-contract/TCP/UDP/listener Rust tests, Native-peer half-close reply proof, 8-process identity, 20 file/App/Lib cases, 8 TCP/Lib pairs, 4 read-stop pairs with 9 controls, 19 resident App/Lib server connections, 1000 resident calls per engine and 8 listener controls; UDP8messages/5calls/3rejects plus11JScontrols/12controlled-retirement-controls; file read8/write7, stream7/buffer10/Core-packing7/malformed-completion6plus1realTCP controls; Bun JS-client half-close is separate characterization, not positive parity; guest trap poisons with no replay, not guest async ABI or generic failed-shutdown qualification" >> "$GITHUB_STEP_SUMMARY"
  browser:
    strategy:
      fail-fast: false
      matrix:
        engine: [chrome, firefox, webkit]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.5.1
      - name: Real browser App Lib and ownership probes
        shell: bash
        env:
          BROWSER: ${{ matrix.engine }}
          SOURCE: ${{ github.sha }}
        run: |
          node host/embedding/browser/server.mjs > "$RUNNER_TEMP/wasmc-browser-url.txt" 2> "$RUNNER_TEMP/wasmc-browser-server.err" &
          server_pid=$!
          cd "$RUNNER_TEMP"
          trap 'npm exec --yes --package=@playwright/cli@0.1.19 -- playwright-cli -s=wasmc-host-ci close || true; kill "$server_pid" || true' EXIT
          npm exec --yes --package=@playwright/cli@0.1.19 -- playwright-cli install-browser "$BROWSER" --with-deps
          url=$(head -n 1 "$RUNNER_TEMP/wasmc-browser-url.txt")
          [[ "$url" == http://127.0.0.1:* ]] || { cat "$RUNNER_TEMP/wasmc-browser-server.err"; exit 1; }
          npm exec --yes --package=@playwright/cli@0.1.19 -- playwright-cli -s=wasmc-host-ci open "$url" --browser "$BROWSER"
          npm exec --yes --package=@playwright/cli@0.1.19 -- playwright-cli -s=wasmc-host-ci --raw eval 'async () => { const receipt=await globalThis.receiptPromise; if(!receipt.accepted) throw Error(receipt.error); return receipt; }' > "$RUNNER_TEMP/wasmc-browser-receipt.json"
          node --input-type=module -e 'import{readFileSync}from"node:fs";import assert from"node:assert/strict";const r=JSON.parse(readFileSync(process.argv[1]));assert.ok(r.accepted&&r.no_replay&&r.trap_poisoned&&r.resource_cleanup&&r.quarantine_retained);assert.equal(r.resident_calls,1000);assert.equal(r.guard_cases,4);assert.equal(r.stop_failure_cases,4);assert.equal(r.supervisor_cases,1);assert.equal(r.completion_snapshot_cases,3);assert.equal(r.driver_write_snapshot_cases,1);assert.equal(r.guard_pool_cases,1);assert.equal(r.kernel_core_cases,4);console.log(JSON.stringify(r));' "$RUNNER_TEMP/wasmc-browser-receipt.json"
          node --input-type=module -e 'import{readFileSync}from"node:fs";import assert from"node:assert/strict";const r=JSON.parse(readFileSync(process.argv[1]));assert.equal(r.quarantine_retirement_cases,1);assert.equal(r.resident_snapshot_cases,3);' "$RUNNER_TEMP/wasmc-browser-receipt.json"
          printf '%s\n' "Source $SOURCE; real $BROWSER; App/Lib1000 resident calls; Core/guard/quarantine probes plus explicit unknown-retirement fence; no raw TCP/mobile claim" >> "$GITHUB_STEP_SUMMARY"
  required:
    if: always()
    needs: [composition, browser, stream-shapes, mobile-compile]
    runs-on: ubuntu-24.04
    steps:
      - env:
          RESULT: ${{ needs.composition.result }}
          BROWSER_RESULT: ${{ needs.browser.result }}
          STREAM_RESULT: ${{ needs.stream-shapes.result }}
          MOBILE_RESULT: ${{ needs.mobile-compile.result }}
        run: test "$RESULT" = success && test "$BROWSER_RESULT" = success && test "$STREAM_RESULT" = success && test "$MOBILE_RESULT" = success
