[package]
name = "wasmc-lib-search-component"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
wasmtime = { version = "=47.0.3", default-features = false, features = ["runtime", "cranelift", "component-model"] }

[lib]
crate-type = ["rlib"]

[workspace]
