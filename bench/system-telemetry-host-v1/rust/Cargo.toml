[package]
name = "wasmc-system-telemetry-zero-host-api"
version = "0.0.1"
edition = "2021"
publish = false

[workspace]

[dependencies]
sysinfo = { version = "=0.39.6", default-features = false, features = ["system", "network"] }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "=0.2.189"
